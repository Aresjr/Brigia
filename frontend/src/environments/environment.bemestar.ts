import packageInfo from '../../package.json';

export const environment = {
  production: true,
  clientName: 'Bem Estar',
  apiUrl: 'https://bemestarbackend.nemeia.com.br',
  version: packageInfo.version
};
